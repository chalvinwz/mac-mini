---
apiVersion: v1
kind: Service
metadata:
  name: notification-server
  namespace: planpal
spec:
  selector: { app: notification-server }
  ports:
    - { name: http, port: 8088, targetPort: http }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-server
  namespace: planpal
spec:
  replicas: 1
  selector:
    matchLabels: { app: notification-server }
  template:
    metadata:
      labels: { app: notification-server }
    spec:
      serviceAccountName: planpal
      automountServiceAccountToken: false
      enableServiceLinks: false
      containers:
        - name: notification-server
          image: registry.10-218-65-27.traefik.me/planpal-notification-server:dev
          imagePullPolicy: Always
          ports:
            - { name: http, containerPort: 8088 }
          envFrom:
            - secretRef: { name: planpal-env }
            - configMapRef: { name: planpal-config }
          startupProbe:
            tcpSocket: { port: http }
            periodSeconds: 2
            failureThreshold: 30
          livenessProbe:
            tcpSocket: { port: http }
            periodSeconds: 15
            failureThreshold: 4
          readinessProbe:
            httpGet: { path: /health, port: http }
            periodSeconds: 10
            failureThreshold: 2
          resources:
            requests: { cpu: 50m, memory: 96Mi }
            limits: { memory: 512Mi }

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: notification-server
  namespace: planpal
spec:
  hostnames: [planpal-api.10-218-65-27.traefik.me]
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  rules:
    - matches:
        - { path: { type: PathPrefix, value: /api/v1/notifications } }
      filters:
        - type: URLRewrite
          urlRewrite:
            path: { type: ReplacePrefixMatch, replacePrefixMatch: /notifications }
      backendRefs:
        - { name: notification-server, port: 8088 }
